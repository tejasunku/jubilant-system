# OpenCode - AI Coding Agent
# https://opencode.ai/
#
# Usage:
#   1. Run: podman compose -f opencode-compose.yml up -d
#   2. Access: http://localhost:4096 (web UI)
#   3. Set password via OPENCODE_SERVER_PASSWORD in .env

services:
  opencode:
    image: localhost/opencode-custom:latest
    container_name: opencode
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:4096:4096"
    volumes:
      - /home/admin/opencode:/workspace
      - /home/admin/opencode/.config/opencode:/workspace/.config/opencode
      - /home/admin/opencode/.local/share/opencode:/workspace/.local/share/opencode
      - /home/admin/opencode/.local/state/opencode:/workspace/.local/state/opencode
      - /home/admin/opencode/.cache/opencode:/workspace/.cache/opencode
    environment:
      - OPENCODE_SERVER_PASSWORD=${OPENCODE_SERVER_PASSWORD:-opencode}
      - OPENCODE_SERVER_USERNAME=admin
      - HOME=/workspace
    restart: unless-stopped
    network_mode: host